/* Task 0.9 — implementation file for the IntArray module.
 *
 * TODO: move (retype, don't paste) your working task 0.8 implementations
 * here. Then build it BY HAND first to see the compile/link stages:
 *
 *   clang -c phase0/intarray.c -o build/intarray.o
 *   clang -c phase0/09_link_test.c -o build/09_test.o
 *   clang build/intarray.o build/09_test.o -o build/p0-09-manual
 *   ./build/p0-09-manual
 *
 * Try deliberately breaking it once: comment out array_push below and
 * rebuild — read the "undefined symbol" linker error you get. That error
 * message will chase you for the rest of your C life; meet it on purpose.
 */
#include <stdlib.h>
#include "intarray.h"

/* TODO: array_init, array_push, array_get, array_free */
